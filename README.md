# How to load this app in dev
## Here is how to make app work in terminal, while development:
1. apply your react changes, use `npm run build`, so now you can run electron in dev mode
2. now you can use `npm run dist:win | npm run dist:linux | npm run dist:mac`, so you'll create a .exe file on your pc
